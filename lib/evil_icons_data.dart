part of evil_icons_flutter;

class EvilIconData extends IconData {
  const EvilIconData(int codePoint)
      : super(
          codePoint,
          fontFamily: 'EvilIcons',
          fontPackage: 'evil_icons_flutter',
        );
}
